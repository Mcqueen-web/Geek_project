This is a python project that uses Groq API and in-built GUI to create a chatbot.
It contains a build that let users run the code with a .exe file directly to interact with the bot without running the code in the terminal.
